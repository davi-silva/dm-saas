services:
  - type: web
    name: dm-saas-backend
    env: Node
    repo: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    port: 5000
    envVars:
      - key: NODE_ENV
        value: production
